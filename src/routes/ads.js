const express = require('express');
const router = express.Router();

// Placeholder routes
router.get('/active', (req, res) => {
  res.json({ message: 'Get active ads - to be implemented' });
});

module.exports = router;
